#!/bin/sh

cd success-factory

# Run ESLint on staged files
echo "🔍 Running ESLint..."
npm run lint --silent || {
  echo "❌ ESLint failed. Please fix errors before committing."
  exit 1
}

# Run color audit
echo "🎨 Running color audit..."
node scripts/audit-colors.js || {
  echo "❌ Color audit failed. Please use semantic color tokens."
  exit 1
}

echo "✅ Pre-commit checks passed!"
exit 0
